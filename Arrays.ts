const numberArr: number[] = [1, 2, 3.12, 12.0, 4, 0.1]; // all must be numbers

// either all of them should be numbers of strings
let string_or_number_Arr: string[] | number[] = [1, 2, 3];
string_or_number_Arr = ["ar", "df", "dfd"];

// combination of number and string
let string_and_number_and_bool_Arr: (string | number | boolean)[] = [
  1,
  "dsf",
  "fd",
  12,
  1.01,
  true,
  false,
];
